<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="training_course.list">
      <field name="name">training_course list</field>
      <field name="model">training_course.training_course</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="training_course.action_window">
      <field name="name">training_course window</field>
      <field name="res_model">training_course.training_course</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="training_course.action_server">
      <field name="name">training_course server</field>
      <field name="model_id" ref="model_training_course_training_course"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="training_course" id="training_course.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="training_course.menu_1" parent="training_course.menu_root"/>
    <menuitem name="Menu 2" id="training_course.menu_2" parent="training_course.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="training_course.menu_1_list" parent="training_course.menu_1"
              action="training_course.action_window"/>
    <menuitem name="Server to list" id="training_course" parent="training_course.menu_2"
              action="training_course.action_server"/>
-->
  </data>
</odoo>
