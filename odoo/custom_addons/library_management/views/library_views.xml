<odoo>
    <!-- Tree View -->
    <record id="view_library_book_tree" model="ir.ui.view">
        <field name="name">library.book.tree</field>
        <field name="model">library.book</field>
        <field name="arch" type="xml">
            <tree decoration-danger="state == 'borrowed'" decoration-success="state == 'available'" decoration-muted="state == 'lost'">
                <field name="name"/>
                <field name="isbn"/>
                <field name="category_id"/>
                <field name="author_ids" widget="many2many_tags"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_library_book_form" model="ir.ui.view">
        <field name="name">library.book.form</field>
        <field name="model">library.book</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name"/>
                        </h1>
                        <h2>
                            <field name="isbn"/>
                        </h2>
                        <field name="condition" widget="priority"/>
                    </div>
                    <group>
                        <group>
                            <field name="category_id" options="{'no_create': True}"/>
                        </group>
                        <group>
                            <field name="author_ids" widget="many2many_tags"/>
                        </group>
                    </group>
                    <notebook>
                        <page name="loans" string="Lịch sử mượn trả">
                            <field name="loan_ids">
                                <tree editable="bottom">
                                    <field name="borrower_name"/>
                                    <field name="borrow_date"/>
                                    <field name="return_date"/>
                                    <field name="is_returned"/>
                                    <field name="duration"/>
                                </tree>
                            </field>
                        </page>
                        <page name="inventory" string="Thông tin Kho" groups="library_management.group_library_librarian">
                            <group>
                                <field name="purchase_price" groups="library_management.group_library_librarian"/>
                                <field name="purchase_date"/>
                                <field name="days_since_purchase"/>
                                <field name="total_loans"/>
                                <field name="notes"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_library_book_search" model="ir.ui.view">
        <field name="name">library.book.search</field>
        <field name="model">library.book</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="isbn"/>
                <filter name="available" string="Sách có sẵn" domain="[('state', '=', 'available')]"/>
                <filter name="draft" string="Sách mới nhập" domain="[('state', '=', 'draft')]"/>
                <group expand="0" string="Group By">
                    <filter name="groupby_category" string="Thể loại" context="{'group_by': 'category_id'}"/>
                    <filter name="groupby_author" string="Tác giả" context="{'group_by': 'author_ids'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Category Tree View -->
    <record id="view_library_category_tree" model="ir.ui.view">
        <field name="name">library.category.tree</field>
        <field name="model">library.category</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!-- Category Form View -->
    <record id="view_library_category_form" model="ir.ui.view">
        <field name="name">library.category.form</field>
        <field name="model">library.category</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Author Tree View -->
    <record id="view_library_author_tree" model="ir.ui.view">
        <field name="name">library.author.tree</field>
        <field name="model">library.author</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="bio"/>
            </tree>
        </field>
    </record>

    <!-- Author Form View -->
    <record id="view_library_author_form" model="ir.ui.view">
        <field name="name">library.author.form</field>
        <field name="model">library.author</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="bio"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Loan Tree View -->
    <record id="view_library_loan_tree" model="ir.ui.view">
        <field name="name">library.loan.tree</field>
        <field name="model">library.loan</field>
        <field name="arch" type="xml">
            <tree>
                <field name="borrower_name"/>
                <field name="borrow_date"/>
                <field name="return_date"/>
                <field name="is_returned"/>
                <field name="duration"/>
                <field name="book_id"/>
            </tree>
        </field>
    </record>

    <!-- Loan Form View -->
    <record id="view_library_loan_form" model="ir.ui.view">
        <field name="name">library.loan.form</field>
        <field name="model">library.loan</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="borrower_name"/>
                        <field name="borrow_date"/>
                        <field name="return_date"/>
                        <field name="is_returned"/>
                        <field name="duration"/>
                        <field name="book_id"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>