<odoo>
    <data>
        <!-- Hotel Room Tree & Form -->
        <record id="view_hotel_room_tree" model="ir.ui.view">
            <field name="name">hotel.room.tree</field>
            <field name="model">hotel.room</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="name"/>
                    <field name="status"/>
                </tree>
            </field>
        </record>
        <record id="view_hotel_room_form" model="ir.ui.view">
            <field name="name">hotel.room.form</field>
            <field name="model">hotel.room</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="status"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Hotel Booking Tree & Form -->
        <record id="view_hotel_booking_tree" model="ir.ui.view">
            <field name="name">hotel.booking.tree</field>
            <field name="model">hotel.booking</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="name"/>
                    <field name="student_id"/>
                    <field name="room_id"/>
                    <field name="checkin_date"/>
                    <field name="checkout_date"/>
                    <field name="state"/>
                </tree>
            </field>
        </record>
        <record id="view_hotel_booking_form" model="ir.ui.view">
            <field name="name">hotel.booking.form</field>
            <field name="model">hotel.booking</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <header>
                            <button name="action_confirm" string="Check-in / Xác nhận" type="object" class="oe_highlight" if="state == 'draft'"/>
                            <button name="action_done" string="Check-out  Thanh toán" type="object" class="oe_highlight" if="state == 'confirmed'" confirm="Bạn có chắc chắn muốn trả phòng và thanh toán?"/>
                            <button name="action_cancel" string="Hủy đơn" type="object" if="state in ('draft','confirmed')"/>
                            <button name="action_draft" string="Đặt lại" type="object" if="state == 'cancel'"/>
                            <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,done,cancel"/>
                        </header>
                        <group>
                            <field name="name"/>
                            <field name="student_id"/>
                            <field name="room_id"/>
                            <field name="checkin_date"/>
                            <field name="checkout_date"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
    </data>
</odoo>
